---
- name: Provision RHEL 9.6 developer machine
  hosts: "{{ target_hosts | default('devmachines') }}"
  become: true
  gather_facts: true
  roles:
    - role: storage
      when: devmachine_storage_setup_enabled | default(false) | bool
    - role: ide
